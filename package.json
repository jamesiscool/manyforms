{
	"private": true,
	"workspaces": [
		"common",
		"renderer",
		"server"
	],
	"scripts": {
		"start": "lerna run --parallel start",
		"build": "lerna run build",
		"deploy": "lerna run deploy"
	},
	"devDependencies": {
		"lerna": "^3.19.0"
	}
}
